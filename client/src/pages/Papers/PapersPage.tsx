import { useEffect,useState } from 'react';
import { Link } from 'react-router-dom';
import type { PaperProject } from '@shared/paper-project.interface';
import { listProjects } from '../../api/paper-projects';

export default function PapersPage(){const[projects,setProjects]=useState<PaperProject[]>([]);const[error,setError]=useState('');const[loading,setLoading]=useState(true);useEffect(()=>{void listProjects().then(setProjects).catch(()=>setError('论文项目加载失败，请重试。')).finally(()=>setLoading(false));},[]);return <main className="max-w-[1200px] mx-auto px-6 py-8"><div className="flex items-center justify-between mb-6"><div><h1 className="text-2xl font-semibold text-slate-800">论文项目</h1><p className="text-sm text-slate-500 mt-1">从研究想法持续推进到分章节正文与修订历史。</p></div><Link to="/papers/new" className="rounded-md bg-blue-600 px-4 py-2 text-sm font-medium text-white">新建论文</Link></div>{loading&&<p className="text-sm text-slate-500">正在加载…</p>}{error&&<p className="rounded-md bg-red-50 p-3 text-sm text-red-600">{error}</p>} {!loading&&!error&&projects.length===0&&<div className="rounded-xl border border-dashed border-slate-300 bg-white p-10 text-center"><p className="text-slate-700">还没有论文项目</p><p className="mt-2 text-sm text-slate-500">无需上传资料，也可以先从 MODEL_ONLY 草稿开始。</p></div>}<div className="grid gap-4 md:grid-cols-2">{projects.map(project=><Link key={project.id} to={`/papers/${project.id}`} className="rounded-xl border border-slate-200 bg-white p-5 hover:border-blue-300"><h2 className="font-semibold text-slate-800">{project.selectedTitle??'未命名论文'}</h2><p className="mt-2 line-clamp-2 text-sm text-slate-500">{project.profile.researchIdea}</p><div className="mt-4 flex justify-between text-xs text-slate-400"><span>{project.profile.paperType}</span><span>版本 {project.lockVersion}</span></div></Link>)}</div></main>}
